---
import PageLayout from '../layouts/PageLayout.astro';
import CTAButton from '../components/CTAButton.astro';

const email = 'hello@joeypcodes.com';
---

<PageLayout
  title="Let’s build something together"
  description="Reach out with the problem you’re solving, and I’ll follow up within one business day."
  eyebrow="Contact"
>
  <div class="flex flex-col gap-8 text-[color:var(--color-text-muted)]">
    <CTAButton href={`mailto:${email}`} variant="primary" size="lg">
      Email {email}
    </CTAButton>
    <p class="max-w-prose">
      Prefer async collaboration? Include context, timelines, and any reference materials. I work best when
      I understand the constraints and desired outcomes up front.
    </p>
    <div class="space-y-3 text-sm">
      <p>Available for:</p>
      <ul class="grid gap-2 md:grid-cols-2">
        <li class="rounded-xl border border-[color:var(--color-border)] bg-[color:var(--color-surface)] px-4 py-3">Azure automation projects</li>
        <li class="rounded-xl border border-[color:var(--color-border)] bg-[color:var(--color-surface)] px-4 py-3">.NET API development</li>
        <li class="rounded-xl border border-[color:var(--color-border)] bg-[color:var(--color-surface)] px-4 py-3">CI/CD pipeline audits</li>
        <li class="rounded-xl border border-[color:var(--color-border)] bg-[color:var(--color-surface)] px-4 py-3">DevOps enablement workshops</li>
      </ul>
    </div>
  </div>
</PageLayout>
